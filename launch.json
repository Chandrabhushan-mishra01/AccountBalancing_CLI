{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Splitwise (gdb)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/main.exe",   // <— change here
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "externalConsole": true,
      "MIMode": "gdb",
      "miDebuggerPath": "C:\\msys64\\ucrt64\\bin\\gdb.exe",  // good to set explicitly on Windows
      "preLaunchTask": "build splitwise"
    }
  ]
}
